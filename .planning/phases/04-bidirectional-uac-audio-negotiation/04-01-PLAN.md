---
phase: 04-bidirectional-uac-audio-negotiation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main/stream_uac.cpp
autonomous: true
requirements: [AUDIO-01, AUDIO-02, AUDIO-03]

must_haves:
  truths:
    - "UAC_PROFILE_FTX1 mic negotiation tries (2ch/24-bit) -> (2ch/16-bit) -> (1ch/24-bit) -> (1ch/16-bit), all at 48kHz, via a real candidate-scan loop instead of the single hardcoded UAC_CHANNELS/UAC_BIT_RESOLUTION candidate"
    - "UAC_PROFILE_FTX1 speaker (TX_CONNECTED) reuses the existing QMX pre-open/suspend-resume machinery with exactly one candidate: 2ch/24-bit/48kHz, matching dds_render_24bit_stereo()"
    - "usb_lib_task() applies the custom FIFO partition (rx=91/nptx=18/ptx=91 lines) for UAC_PROFILE_FTX1 as well as UAC_PROFILE_QMX, starting from the QMX split per D-06"
    - "rx_fifo_lines + nptx_fifo_lines + ptx_fifo_lines never exceeds 200 for any profile reaching the custom-FIFO branch (ESP32-S3 DWC_OTG hard HAL_ASSERT ceiling, ESP_IDF usb_dwc_hal.c)"
    - "cp210x_try_open() remains unconditional for UAC_PROFILE_FTX1, independent of mic negotiation outcome (does not regress the 03-01 gap-closure fix)"
  artifacts:
    - path: "main/stream_uac.cpp"
      provides: "FTX-1 mic candidate-scan branch, widened speaker guard, widened FIFO partition guard"
      contains: "UAC_PROFILE_FTX1"
  key_links:
    - from: "uac_lib_task() RX_CONNECTED handler add_candidate sequence"
      to: "s_format (negotiated mic format)"
      via: "for-loop trying candidates[] in D-02 order, first uac_host_device_start() success wins"
      pattern: "s_profile == UAC_PROFILE_FTX1"
    - from: "uac_lib_task() TX_CONNECTED handler guard"
      to: "existing QMX speaker pre-open block (lines 696-768, unchanged)"
      via: "widened if-condition admitting UAC_PROFILE_FTX1"
      pattern: "s_profile != UAC_PROFILE_QMX && s_profile != UAC_PROFILE_FTX1"
    - from: "usb_lib_task() FIFO partition guard"
      to: "host_config.fifo_settings_custom"
      via: "widened if-condition admitting UAC_PROFILE_FTX1"
      pattern: "s_profile == UAC_PROFILE_QMX || s_profile == UAC_PROFILE_FTX1"
---

<objective>
Extend the two already-proven `stream_uac.cpp` patterns (mic candidate-scan, QMX speaker pre-open) and the FIFO custom-partition config so `UAC_PROFILE_FTX1` gets real runtime format negotiation on mic RX, reuses the QMX speaker TX pre-open path, and is included in the custom FIFO budget — per CONTEXT.md's locked decisions D-01 through D-09. This is a pure mechanical extension of existing profile-gated branches in one file; no new subsystem, no new dependency.

Purpose: Satisfy AUDIO-01 (mic runtime negotiation), AUDIO-02 (speaker TX reuse), AUDIO-03 (FIFO re-tune) in code. Physical validation against the real FTX-1 (actual negotiated format, clean TX tone, no FIFO overrun across cycles) happens in the follow-on checkpoint plan (04-02).
Output: A firmware build in which selecting the FTX-1 profile scans real mic candidates, pre-opens the speaker with the QMX-identical fixed format, and reaches the custom 91/18/91-line FIFO partition — all gated on `s_profile`, all within the ESP32-S3's fixed 200-line ceiling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-bidirectional-uac-audio-negotiation/04-RESEARCH.md
@.planning/phases/04-bidirectional-uac-audio-negotiation/04-PATTERNS.md
@.planning/phases/04-bidirectional-uac-audio-negotiation/04-CONTEXT.md

<interfaces>
<!-- Exact current code, read directly this session from main/stream_uac.cpp -->

usb_lib_task() FIFO guard, lines 413-426 (BEFORE):
- `if (s_profile == UAC_PROFILE_QMX) { host_config.fifo_settings_custom.rx_fifo_lines = 91; ...nptx_fifo_lines = 18; ...ptx_fifo_lines = 91; ESP_LOGI(...policy=qmx-custom...); } else { ESP_LOGI(...policy=default...); }`
- Widen the condition ONLY: `if (s_profile == UAC_PROFILE_QMX || s_profile == UAC_PROFILE_FTX1)`. Values (91/18/91) and log format string are unchanged — `profile_name(s_profile)` already prints whichever profile is active.

uac_lib_task() RX_CONNECTED handler, lines 580-596 (BEFORE):
- `uac_host_stream_config_t candidates[4]; int candidate_count = 0; auto add_candidate = [&](uint8_t ch, uint8_t bits) { candidates[candidate_count].channels = ch; ...bit_resolution = bits; ...sample_freq = UAC_SAMPLE_RATE; ...flags = 0; candidate_count++; };`
- `if (s_profile == UAC_PROFILE_GENERIC_USB) { add_candidate(1,24); add_candidate(1,16); add_candidate(2,24); add_candidate(2,16); } else { add_candidate(UAC_CHANNELS, UAC_BIT_RESOLUTION); }`
- `candidates[4]` array size is exactly enough for a 4-entry FTX1 branch too — do not resize.

uac_lib_task() failure-path, lines 617-629 (BEFORE):
- `if (!started) { if (s_profile == UAC_PROFILE_GENERIC_USB) { ..."No 48k UAC mic format"...; } else { ..."Format not supported"...; } uac_host_device_close(handle); continue; }`

uac_lib_task() TX_CONNECTED handler, lines 687-694 (BEFORE):
- `} else if (evt.driver.event == UAC_HOST_DRIVER_EVENT_TX_CONNECTED) { if (s_profile != UAC_PROFILE_QMX) { ESP_LOGI(..."Speaker connected ignored..."...); continue; } // pre-open logic lines 696-768 UNCHANGED`

cp210x_try_open() dispatch, lines 565-574 (already correct since 03-01 Task 3 gap-closure — do NOT move):
- `if (s_profile == UAC_PROFILE_FTX1) { cp210x_try_open(); }` — runs unconditionally right after `uac_host_printf_device_param(handle);`, before candidate negotiation. A stale comment here claims the FTX-1's strict single-candidate approach "reliably fails on real hardware" — this becomes inaccurate once the mic branch multi-candidate-scans; update the comment text only, do not change the unconditional call itself (CAT-1 availability must never depend on mic negotiation outcome).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UAC_PROFILE_FTX1 mic candidate-scan branch (D-01/D-02)</name>
  <files>main/stream_uac.cpp</files>
  <read_first>
    - main/stream_uac.cpp lines 560-630 (uac_lib_task RX_CONNECTED handler: cp210x_try_open dispatch, add_candidate lambda, the GENERIC_USB/else if-chain, the downstream negotiation for-loop, and the failure-path branch)
    - .planning/phases/04-bidirectional-uac-audio-negotiation/04-PATTERNS.md Pattern 1 (exact insertion diff and failure-path discretion note)
    - .planning/phases/04-bidirectional-uac-audio-negotiation/04-CONTEXT.md D-01/D-02/D-03 (locked try-order, no downstream resampler changes)
  </read_first>
  <action>
    Insert a new `else if (s_profile == UAC_PROFILE_FTX1) { ... }` arm between the existing `if (s_profile == UAC_PROFILE_GENERIC_USB) { ... }` arm and the final `else { add_candidate(UAC_CHANNELS, UAC_BIT_RESOLUTION); }` (which becomes QMX-only after this change) at stream_uac.cpp:589-596. Inside the new arm, call `add_candidate(2, 24); add_candidate(2, 16); add_candidate(1, 24); add_candidate(1, 16);` in exactly that order (D-02: QMX-parity-shaped formats first, degrading toward GENERIC_USB's mono/16-bit fallback). Add a one-line comment above the arm citing D-02's rationale (FTX-1 architecturally closer to QMX than to a generic USB adapter). Do not touch the `add_candidate` lambda, `candidates[4]` array size, or the downstream `for (int i = 0; i < candidate_count; ++i)` negotiation loop (lines 598-615) — both are already generic over whatever candidate list was populated and require zero changes per D-03.

    In the failure-path branch (lines 617-629), widen the `if (s_profile == UAC_PROFILE_GENERIC_USB)` check to `if (s_profile == UAC_PROFILE_GENERIC_USB || s_profile == UAC_PROFILE_FTX1)` so a failed FTX-1 scan logs "No 48k UAC mic format" (accurate for a scanning profile) instead of the generic "Format not supported" message — this is Claude's Discretion per CONTEXT.md, applied for message accuracy, not a locked decision.

    Update the stale comment at cp210x_try_open()'s FTX-1 dispatch (lines 565-574) that claims "the FTX-1's mic-only advertises 16-bit while this profile's sole candidate is 24-bit, so negotiation reliably fails on real hardware" — this is no longer accurate once multi-candidate scanning lands. Replace with a comment noting cp210x_try_open() runs unconditionally, independent of mic negotiation outcome, so CAT-1 availability never depends on audio negotiation success (do not change the unconditional call itself).
  </action>
  <verify>
    <automated>grep -n "else if (s_profile == UAC_PROFILE_FTX1)" main/stream_uac.cpp && grep -c "add_candidate(2, 24)" main/stream_uac.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "else if (s_profile == UAC_PROFILE_FTX1)" main/stream_uac.cpp` finds exactly one occurrence, positioned between the `GENERIC_USB` arm and the final `else` in the RX_CONNECTED candidate-selection if-chain
    - Within that arm, `add_candidate(2, 24)`, `add_candidate(2, 16)`, `add_candidate(1, 24)`, `add_candidate(1, 16)` appear in that exact order (verify by reading the arm's body, top to bottom)
    - The final `else` arm (now QMX-only after the FTX1 carve-out) still contains `add_candidate(UAC_CHANNELS, UAC_BIT_RESOLUTION)` unchanged
    - The failure-path `if` at line ~618 reads `if (s_profile == UAC_PROFILE_GENERIC_USB || s_profile == UAC_PROFILE_FTX1)`
    - The `add_candidate` lambda body, `candidates[4]` declaration, and the negotiation for-loop (lines ~598-615) are byte-for-byte unchanged from before this task
    - `cp210x_try_open();` at the FTX-1 dispatch (~line 573) remains an unconditional call inside `if (s_profile == UAC_PROFILE_FTX1)`, positioned before the candidate-scan block, not inside any `if (started)`/`if (!started)` branch
  </acceptance_criteria>
  <done>UAC_PROFILE_FTX1 mic negotiation runs a real 4-candidate scan in D-02's exact order; a failed scan logs the GENERIC_USB-style "No 48k UAC mic format" message; cp210x_try_open() remains unconditional and its stale comment is corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Widen speaker (TX_CONNECTED) guard for UAC_PROFILE_FTX1 (D-04/D-05)</name>
  <files>main/stream_uac.cpp</files>
  <read_first>
    - main/stream_uac.cpp lines 687-768 (uac_lib_task TX_CONNECTED handler: guard, s_spk_addr/iface capture, pre-open block with fixed scfg = {2,24,48000,FLAG_STREAM_SUSPEND_AFTER_START}, pump buffer + writer task allocation)
    - main/dds_q15.h (confirm dds_render_24bit_stereo() is fixed 24-bit/stereo — no signature change this task)
    - .planning/phases/04-bidirectional-uac-audio-negotiation/04-PATTERNS.md Pattern 2 (exact guard-only diff)
    - .planning/phases/04-bidirectional-uac-audio-negotiation/04-CONTEXT.md D-04/D-05 (single fixed candidate, no multi-format renderer)
  </read_first>
  <action>
    Widen the single guard condition at stream_uac.cpp:688 from `if (s_profile != UAC_PROFILE_QMX)` to `if (s_profile != UAC_PROFILE_QMX && s_profile != UAC_PROFILE_FTX1)`. Make no other change in this handler: the `s_spk_addr`/`s_spk_iface` capture, the `uac_host_device_open()` call, the fixed `scfg = {2, 24, 48000, FLAG_STREAM_SUSPEND_AFTER_START}` (lines 728-732), the pump-buffer allocation, and the `spk_writer_task` creation (lines 742-765) all execute identically regardless of which profile (QMX or FTX1) reached this point — do not parameterize `scfg`, do not add a second candidate, do not add an FTX1-specific pre-open path. D-04 mandates exactly one candidate matching `dds_render_24bit_stereo()`'s fixed 24-bit/stereo output; D-05 explicitly defers building any fallback until hardware validation (04-02) shows it's needed.
  </action>
  <verify>
    <automated>grep -n "s_profile != UAC_PROFILE_QMX && s_profile != UAC_PROFILE_FTX1" main/stream_uac.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "if (s_profile != UAC_PROFILE_QMX && s_profile != UAC_PROFILE_FTX1)" main/stream_uac.cpp` finds exactly one occurrence, inside the `UAC_HOST_DRIVER_EVENT_TX_CONNECTED` branch
    - `scfg.channels`, `scfg.bit_resolution`, `scfg.sample_freq` remain `2`, `24`, `48000` respectively — no second scfg/candidate added, no per-profile branching introduced inside the pre-open block
    - The pump-buffer allocation and `spk_writer_task` creation blocks (originally lines 742-765) are byte-for-byte unchanged
    - No new file or function is added for FTX-1-specific speaker handling (D-05: any fallback is deferred to hardware validation, not pre-built)
  </acceptance_criteria>
  <done>UAC_PROFILE_FTX1 speaker connections now reach the existing QMX pre-open/suspend-resume block unmodified, using the single fixed 2ch/24-bit/48kHz candidate.</done>
</task>

<task type="auto">
  <name>Task 3: Widen FIFO partition guard for UAC_PROFILE_FTX1 and verify the 200-line budget (D-06/D-07/AUDIO-03)</name>
  <files>main/stream_uac.cpp</files>
  <read_first>
    - main/stream_uac.cpp lines 407-427 (usb_lib_task: host_config setup, the QMX-only custom FIFO branch, the else default-policy branch)
    - .planning/phases/04-bidirectional-uac-audio-negotiation/04-RESEARCH.md "Pattern 3: FIFO partition guard widening + budget arithmetic" and "Pitfall 1: FIFO over-budget causes boot-time HAL_ASSERT" (hard 200-line ceiling, rx+nptx+ptx <= 200 enforced via HAL_ASSERT in usb_dwc_hal_set_fifo_config, components/hal/usb_dwc_hal.c)
    - .planning/phases/04-bidirectional-uac-audio-negotiation/04-CONTEXT.md D-06/D-07 (QMX split as FTX-1 starting point; empirical adjustment deferred to hardware-checkpoint plan)
  </read_first>
  <action>
    Widen the guard at stream_uac.cpp:413 from `if (s_profile == UAC_PROFILE_QMX)` to `if (s_profile == UAC_PROFILE_QMX || s_profile == UAC_PROFILE_FTX1)`. Keep `rx_fifo_lines = 91`, `nptx_fifo_lines = 18`, `ptx_fifo_lines = 91` unchanged as the D-06 analytical starting point (sum = 91+18+91 = 200, exactly the ESP32-S3 DWC_OTG ceiling with zero headroom — do not increase any of the three values in this task; that adjustment is explicitly deferred to the 04-02 hardware-checkpoint plan per D-07, which must re-verify the sum stays <=200 if any value changes). Update the existing comment block (lines 414-418) to note it now also applies to `UAC_PROFILE_FTX1`, and that FTX-1 additionally carries CP210x bulk CAT traffic sharing the `nptx_fifo_lines` pool — flag this as the item D-07 requires empirical validation on for the hardware-checkpoint plan. The `ESP_LOGI(TAG, "USB FIFO profile=%s policy=qmx-custom rx=91 nptx=18 ptx=91", profile_name(s_profile));` log line format string is unchanged (profile_name() already reports whichever profile triggered this branch).
  </action>
  <verify>
    <automated>grep -n "s_profile == UAC_PROFILE_QMX || s_profile == UAC_PROFILE_FTX1" main/stream_uac.cpp && grep -c "rx_fifo_lines   = 91" main/stream_uac.cpp</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "if (s_profile == UAC_PROFILE_QMX || s_profile == UAC_PROFILE_FTX1)" main/stream_uac.cpp` finds exactly one occurrence, inside `usb_lib_task()`
    - `rx_fifo_lines`, `nptx_fifo_lines`, `ptx_fifo_lines` remain `91`, `18`, `91` respectively (arithmetic check: 91 + 18 + 91 = 200, which is `<= 200` — the ESP32-S3 DWC_OTG hard ceiling verified in 04-RESEARCH.md against `usb_dwc_hal.c`)
    - No other `usb_host_config_t` field is modified in this task
    - The updated comment block explicitly notes the sum-<=200 constraint and that CP210x bulk CAT traffic sharing `nptx_fifo_lines` is the item requiring empirical hardware validation (D-07), not something resolved by this task alone
  </acceptance_criteria>
  <done>usb_lib_task() applies the 91/18/91-line custom FIFO partition for both UAC_PROFILE_QMX and UAC_PROFILE_FTX1; the sum remains exactly 200 (<=200, the ESP32-S3 hard ceiling); the comment flags CP210x bulk CAT as the empirical-validation item for the hardware-checkpoint plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| firmware (usb_lib_task/uac_lib_task) → ESP32-S3 DWC_OTG USB-OTG HAL | Firmware-supplied FIFO line-count configuration crosses into `usb_dwc_hal_set_fifo_config()`, which `HAL_ASSERT`s on an invalid (over-budget) config. This is a self-inflicted local resource-configuration boundary, not a network or multi-tenant boundary — no untrusted external input is involved. |
| FTX-1 USB Audio descriptor → uac_host_device_start() candidate negotiation | Bounds-checked entirely inside the vendored `espressif/usb_host_uac` component; this phase only supplies candidate *values* (channel/bit-depth), not new descriptor-parsing code. |

This phase is offline embedded firmware talking to a locally-plugged, physically-trusted USB radio with no network surface (per ARCHITECTURE.md: "None (local device). CAT radio control assumes trusted connection."). Most ASVS categories are N/A; the only realistic risk class is local availability (DoS-shaped), not confidentiality/integrity/authentication.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service (self-inflicted boot crash) | usb_lib_task() FIFO partition guard (Task 3) | mitigate | Keep rx=91/nptx=18/ptx=91 (sum=200) unchanged this task; any future increase to one field MUST be paired with an equal-or-greater decrease elsewhere in the same change, re-verified arithmetically against the fixed 200-line ceiling before flashing — an over-budget config triggers HAL_ASSERT at usb_host_install() time, not a catchable esp_err_t. |
| T-04-02 | Tampering (malformed/unexpected UAC descriptor) | uac_lib_task() mic candidate-scan (Task 1) | accept | Relies entirely on espressif/usb_host_uac's own bounds-checked descriptor parsing (uac_host_get_device_alt_param, uac_host_device_start); this phase adds only candidate values, no new descriptor-walking code. A malformed descriptor causes a negotiation failure (existing graceful-degradation path), not a crash. |
| T-04-03 | Denial of Service (speaker pre-open failure leaves no TX capability for the session) | uac_lib_task() TX_CONNECTED speaker pre-open (Task 2) | accept | Intentional existing one-shot pre-open design (no retry within a USB attach cycle) per D-08/D-09; degrades to RX-only mode via s_status_string, matching ARCHITECTURE.md's documented "Audio underrun: Mute speaker, continue decoding" philosophy — not a new risk introduced by widening the guard. |
</threat_model>

<verification>
- `grep -n "else if (s_profile == UAC_PROFILE_FTX1)" main/stream_uac.cpp` shows the mic candidate-scan branch present in the RX_CONNECTED handler.
- `grep -n "if (s_profile != UAC_PROFILE_QMX && s_profile != UAC_PROFILE_FTX1)" main/stream_uac.cpp` shows the speaker guard widened.
- `grep -n "if (s_profile == UAC_PROFILE_QMX || s_profile == UAC_PROFILE_FTX1)" main/stream_uac.cpp` shows the FIFO guard widened.
- Manual arithmetic check: rx_fifo_lines (91) + nptx_fifo_lines (18) + ptx_fifo_lines (91) = 200 <= 200 (ESP32-S3 DWC_OTG ceiling).
- Build verification (`idf.py build` with -Werror) is deferred to plan 04-02 Task 1 (ESP-IDF toolchain not on PATH in executor Bash sessions, same caveat as Phases 2/3).
</verification>

<success_criteria>
- UAC_PROFILE_FTX1 mic negotiation scans 4 real candidates in D-02's locked order instead of one hardcoded candidate.
- UAC_PROFILE_FTX1 speaker connections reuse the QMX pre-open/suspend-resume machinery unmodified, with the single fixed 2ch/24-bit/48kHz candidate.
- usb_lib_task()'s custom FIFO partition applies to UAC_PROFILE_FTX1 as well as UAC_PROFILE_QMX, with the 91/18/91 split summing to exactly 200 (the hard ESP32-S3 ceiling, verified arithmetically, not left to a runtime check).
- cp210x_try_open() remains unconditional, independent of mic negotiation outcome.
</success_criteria>

<output>
After completion, create `.planning/phases/04-bidirectional-uac-audio-negotiation/04-01-SUMMARY.md`.
</output>
